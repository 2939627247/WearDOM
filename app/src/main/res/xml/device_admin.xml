<?xml version="1.0" encoding="utf-8"?>
<!--
    Device Owner apps are granted all DevicePolicyManager permissions automatically
    by the system — <uses-policies> entries are not required for DO apps.
    The empty <device-admin> tag is still needed for the manifest receiver declaration.
-->
<device-admin />
